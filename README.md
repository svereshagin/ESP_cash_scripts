# Автоматизация развертывания TSPIOT на TinyCore Linux

Полное решение для автоматической установки и настройки TSPIOT на дистрибутиве TinyCore Linux с логгированием и обработкой ошибок.

## 📋 Содержание

- [Возможности](#возможности)
- [Для кого это решение?](#для-кого-это-решение)
- [Предварительные требования](#предварительные-требования)
- [Быстрый старт](#быстрый-старт)
- [Работа на сервере](#работа-на-сервере)
- [Полный цикл установки](#полный-цикл-установки)
- [Конфигурация](#конфигурация)
- [Структура проекта](#структура-проекта)
- [Документация](#документация)
- [Отладка и логирование](#отладка-и-логирование)
- [Частые проблемы и решения](#частые-проблемы-и-решения)
- [Поддержка и обратная связь](#поддержка-и-обратная-связь)

---

## Возможности

Система предоставляет комплексное решение для автоматизации установки TSPIOT со следующими возможностями:

### Основные функции
- **Автоматическая установка** TSPIOT на TinyCore Linux без необходимости ручной работы
- **Настройка ESM** (Электронной Системы Маркировки)
- **Интеграция с ГИС МТ** (Государственная Информационная Система Маркировки и Трекинга)
- **Настройка ЛМ ЧЗ** (Локального Модуля Честного Знака)
- **Проверка работоспособности** приложения
- **Автозагрузка** сервисов при старте системы
- **Генерация отчетов** и логов установки

### Дополнительные преимущества
- Поддержка двух режимов работы: интерактивный и через флаги
- Автоматическая обработка ошибок и восстановление
- Детальное логирование всех этапов установки
- Интеграция с существующей инфраструктурой

---
## Для кого это решение?

| Роль пользователя | Что получит |
|------------------|-------------|
| **Администратор кассы** | Быстрая установка без глубоких знаний Linux |
| **Технический специалист** | Автоматизация рутинных задач, логирование |
| **Разработчик** | Готовый инструмент для развертывания на клиентских системах |
| **Тестировщик** | Воспроизводимые сценарии установки |

## Предварительные требования
Перед началом установки убедитесь, что:

1. **Аппаратное обеспечение:**
   - Сервер/касса с установленным **TinyCore Linux** / **Ubuntu**

2. **Программное обеспечение:**
   - Права суперпользователя (root/sudo)
   - Доступ к интернету для загрузки зависимостей
   - Установленный curl или wget
3. 
4. **Бизнес-требования:**
   - **Открытая смена на кассе** [обязательное условие]
   - Сетевой доступ к ЛМ ЧЗ
   - Настроенные сетевые подключения

## ⚡ Быстрый старт
Работать со скриптами можно как удалённо, так и непосредственно на сервере.
Далее будет предоставлена информация по работе со скриптами непосредственно на сервере.

### Способ 1: Прямое копирование и запуск (рекомендуется)

```bash
# Скачайте скрипт напрямую
curl -O https://raw.githubusercontent.com/svereshagin/ESP_cash_scripts/main/install_esm_tinycore.sh

# Сделайте исполняемым
chmod +x install_esm_tinycore.sh

# Запустите в интерактивном режиме
sudo ./install_esm_tinycore.sh --default
```

### Способ 2: Клонирование репозитория (для расширенной работы)

```bash
# Клонирование репозитория
git clone https://github.com/svereshagin/ESP_cash_scripts
cd ESP_cash_scripts

# Настройка прав доступа
chmod +x *.sh
chmod +x scripts_per_host/*.sh
chmod +x scripts_remote/*.sh

# Настройка параметров через .env файл
nano .env
```

Пример содержимого файла .env:
```
TINYCORE_SERVER_IP=10.9.130.187
TINYCORE_USERNAME=tc
PASSWORD=324012

LM_CZ_ADDRESS=10.9.130.12
LM_CZ_PORT=50063
LM_CZ_LOGIN=admin
LM_CZ_PASSWORD=admin

GISMT_ADDRESS=https://ts-reg.crpt.ru:19100
COMPATIBILITY_MODE=false
ALLOW_REMOTE_CONNECTION=true
```

### Способ 3: Быстрый запуск с флагами

```bash
sudo ./install_esm_tinycore.sh \
    --LM_CZ_ADDRESS "10.9.130.12" \
    --LM_CZ_PORT "50063" \
    --LM_CZ_LOGIN "admin" \
    --LM_CZ_PASSWORD "admin" \
    --COMPATIBILITY_MODE "false" \
    --ALLOW_REMOTE_CONNECTION "true"
```

---


## Работа на сервере

### Видео-демонстрации запуска скрипта
Перед началом работы рекомендуем посмотреть демонстрации:

| Режим | Ссылка | Описание | Длительность |
|-------|--------|----------|--------------|
| Интерактивный | [asciinema.org/a/tFDYIAQKkdhQXt9G](https://asciinema.org/a/tFDYIAQKkdhQXt9G) | Пошаговый мастер установки с запросом параметров | 8 минут |
| Через флаги | [asciinema.org/a/EbNPOCF6tsdUAypX](https://asciinema.org/a/EbNPOCF6tsdUAypX) | Быстрый запуск с заранее заданными параметрами | 3 минуты |

---
### Пошаговая инструкция создания и запуска скрипта

#### Шаг 1: Создание файла скрипта
```bash
# Создайте файл через редактор vi (доступен на TinyCore)
vi install_esm.sh
```

#### Шаг 2: Вставка содержимого скрипта
1. Нажмите клавишу `I` для входа в режим редактирования
2. Вставьте содержимое скрипта одним из способов:
   - **В PuTTY:** Правая кнопка мыши или `Shift + Insert`
   - **В терминале Linux:** `Ctrl + Shift + V`
3. Основные скрипты для вставки:
   - `install_esm_tinycore.sh` - основной скрипт установки ESM
   - Скрипты из `scripts_per_host/` - дополнительные утилиты проверки

#### Шаг 3: Сохранение и выход из редактора
1. Нажмите клавишу `ESC` для выхода из режима редактирования
2. Введите `:wq` и нажмите Enter:
   - `:` - активация командного режима
   - `w` - сохранение (write)
   - `q` - выход (quit)

   **Внимание:** Язык ввода должен быть английским, иначе команды не сработают.

#### Шаг 4: Запуск скрипта

**Вариант A: Интерактивный режим (рекомендуется для новичков)**
```bash
sh install_esm.sh --default
```
В этом режиме скрипт будет запрашивать все необходимые параметры пошагово.

**Вариант B: Запуск с флагами**
```bash
sh install_esm.sh \
    --LM_CZ_ADDRESS "10.9.130.12" \
    --LM_CZ_PORT "50063" \
    --LM_CZ_LOGIN "admin" \
    --LM_CZ_PASSWORD "admin" \
    --GISMT_ADDRESS "https://ts-reg.crpt.ru:19100" \
    --COMPATIBILITY_MODE "false" \
    --ALLOW_REMOTE_CONNECTION "true"
```
#### Управление редактором vi (полезные команды)
```bash
# Если нужно выйти без сохранения
ESC -> :q!

# Если нужно сохранить под другим именем
ESC -> :w новое_имя.sh

# Просмотр содержимого файла
cat install_esm.sh

# Удаление файла
rm install_esm.sh

# Изменение прав доступа
chmod +x install_esm.sh
```

---

## 🔄 Полный цикл установки

Скрипт выполняет установку в несколько этапов, каждый из которых подробно логируется:

### Этап 1: Подготовка и проверка
- Проверка наличия необходимых пакетов (curl, wget, netcat)
- Проверка сетевого подключения
- Проверка прав доступа (требуются права суперпользователя)
- Проверка состояния кассы (открыта ли смена)
- Проверка свободного дискового пространства

### Этап 2: Загрузка и копирование файлов
- Загрузка TSPIOT на дистрибутив TinyCore Linux
- Создание необходимых директорий
- Настройка автозапуска через `onboot.lst`
- Установка дополнительных пакетов и зависимостей
- Конфигурация системных служб

### Этап 3: Установка ESM
- Установка Электронной Системы Маркировки
- Настройка служб ESM (esm-orchestrator)
- Проверка работоспособности установленных компонентов
- Регистрация в системных службах

### Этап 4: Перезагрузка и инициализация
- Безопасная перезагрузка системы
- Мониторинг запуска кассы после перезагрузки
- Проверка доступности сетевых сервисов
- Тестирование подключения к внешним системам

### Этап 5: Создание экземпляра ESM
- Инициализация экземпляра ESM
- Настройка параметров подключения
- Проверка работоспособности экземпляра
- Получение идентификатора TSPIOT

### Этап 6: Настройка ЛМ ЧЗ
- Конфигурация подключения к Локальному Модулю Честного Знака
- Настройка аутентификации (логин/пароль)
- Тестирование сетевого подключения к ЛМ ЧЗ
- Верификация настроек

### Этап 7: Настройка ГИС МТ
- Регистрация в системе ГИС МТ
- Настройка API-подключения
- Тестирование соединения с серверами ГИС МТ
- Подтверждение успешной интеграции

### Этап 8: Проверка работоспособности
- Валидация тестовой марки для проверки работоспособности системы
- Тестирование всех компонентов в комплексе
- Генерация отчета о тестировании
- Подтверждение готовности системы к работе

### Этап 9: Финальная настройка и отчет
- Создание логов установки
- Генерация полного отчета о результатах установки
- Сохранение конфигурации для последующего использования
- Уведомление об успешном завершении установки

---

## Конфигурация

### Основные параметры установки

Параметры можно задавать через .env файл или через флаги командной строки:

| Параметр | Описание | Обязательный | Значение по умолчанию | Пример |
|----------|----------|--------------|----------------------|---------|
| `COMPATIBILITY_MODE` | Режим совместимости со старыми версиями | Нет | `"false"` | `"true"` |
| `ALLOW_REMOTE_CONNECTION` | Разрешить удаленные подключения к сервису | Нет | `"true"` | `"false"` |
| `LM_CZ_ADDRESS` | IP-адрес Локального Модуля Честного Знака | Да | - | `"10.9.130.12"` |
| `LM_CZ_PORT` | Порт ЛМ ЧЗ | Нет | `"50063"` | `"50063"` |
| `LM_CZ_LOGIN` | Логин для доступа к ЛМ ЧЗ | Нет | `"admin"` | `"admin"` |
| `LM_CZ_PASSWORD` | Пароль для доступа к ЛМ ЧЗ | Нет | `"admin"` | `"P@ssw0rd123"` |
| `GISMT_ADDRESS` | Адрес сервера ГИС МТ | Нет | `"https://ts-reg.crpt.ru:19100"` | `"https://test.gismt.ru:19100"` |

### Настройка через .env файл

Создайте файл .env в корневой директории проекта:

```bash
nano .env
```

Добавьте следующие параметры:
```
TINYCORE_SERVER_IP=10.9.130.187
TINYCORE_USERNAME=tc
PASSWORD=324012

LM_CZ_ADDRESS=10.9.130.12
LM_CZ_PORT=50063
LM_CZ_LOGIN=admin
LM_CZ_PASSWORD=admin

GISMT_ADDRESS=https://ts-reg.crpt.ru:19100
COMPATIBILITY_MODE=false
ALLOW_REMOTE_CONNECTION=true
```

## Структура проекта

### Основные файлы репозитория

```
ESP_cash_scripts/
├── esm_1.5.0.0_tinycore8.run      # Установочный файл ESM (скачивается автоматически)
├── install_esm_tinycore.sh        # Основной скрипт установки ESM на сервере
├── main.sh                        # Главный скрипт, выполняющий полный цикл установки
└── send_esm_script.sh             # Скрипт для отправки файлов на удаленный сервер
```

### Директория scripts_remote/

Содержит скрипты для выполнения задач с удаленного хоста (вашего компьютера):

```
scripts_remote/
├── check_cash_status.sh           # [В разработке] Проверка статуса кассы
├── check_control_module_config.sh  # Проверка конфигурационных файлов (для ГИС МТ)
├── check_mark.sh                  # Утилита проверки марки на сервере
├── connect_to_server.sh           # Утилита для подключения к серверу
├── get_configuration_from_server.sh # Скачивание конфигурации с сервера
└── get_logs_from_server.sh        # Скачивание логов с сервера
```

### Директория scripts_per_host/

Содержит скрипты для выполнения непосредственно на сервере TinyCore:

```
scripts_per_host/
├── check_mark.sh                  # Проверка марки на сервере
└── diagnosis_config.sh             # Диагностика конфигурации системы
```



## Документация

### TSPIOT
Подробная документация по TSPIOT доступна по ссылке:  
[https://docs.google.com/document/d/1pOC0WNUJD6R7F4Z9Pl_unEJ3C0RYLX12O7Flnj9tUBA/edit?tab=t.0#heading=h.tv4326vlf7jz]

### API CIS
Подробная документация по API CIS доступна по ссылке:  
[https://crystals.atlassian.net/wiki/spaces/SR10SUPPORT/pages/434504459/SetRetail10+Linux+TinyCore](https://crystals.atlassian.net/wiki/spaces/SR10SUPPORT/pages/434504459/SetRetail10+Linux+TinyCore)

### Команды Linux TinyCore
Справочник по кассовым командам доступен в документации:  
[SetRetail 10 Linux TinyCore - Кассовые команды](https://crystals.atlassian.net/wiki/spaces/SR10SUPPORT/pages/434504459/SetRetail10+Linux+TinyCore?showComments=true&showCommentArea=true#SetRetail10%25E2%2596%25AA%25EF%25B8%258F%25D0%259A%25D0%25B0%25D1%2581%25D1%2581%25D0%25B0%25E2%2597%25BE%25EF%25B8%258F%25D0%259A%25D0%25BE%25D0%25BC%25D0%25B0%25D0%25BD%25D0%25B4%25D1%258BLinuxTinyCore-%25D0%259A%25D0%25B0%25D1%2581%25D1%2581%25D0%25BE%25D0%25B2%25D1%258B%25D0%25B5%25D0%25BA%25D0%25BE%25D0%25BC%25D0%25B0%25D0%25BD%25D0%25B4%25D1%258B-cash%5B....%5D)

### Демонстрация работы
Посмотреть полный цикл работы программы можно на asciinema:  
[https://asciinema.org/a/qwgqJhUvtO0sOiO3](https://asciinema.org/a/qwgqJhUvtO0sOiO3)

## Отладка и логирование

### Тестирование работоспособности системы

После установки рекомендуется выполнить следующие проверки:

```bash
# 1. Проверка марки на сервере (интеграционный тест)
sh scripts_per_host/check_mark.sh

# 2. Проверка подключения к ЛМ ЧЗ
nc -zv 10.9.130.12 50063
```


# Проверка сетевых подключений(выполняется на сервере)
```shell
netstat -tlnp | grep -E '(51401|50063)'
```
---

**Примечание**: Убедитесь, что перед запуском установки на кассе открыта смена, и все сетевые подключения настроены корректно.


### Установка sshpass для подключения к серверам

**Для Ubuntu/Debian:**
```bash
sudo apt-get update
sudo apt-get install sshpass
```

**Для macOS (через Homebrew):**
```bash
# Установите Homebrew если еще не установлен:
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Установите sshpass:
brew install https://raw.githubusercontent.com/kadwanev/bigboybrew/master/Library/Formula/sshpass.rb
```

**Альтернативный способ для macOS:**
```bash
# Скачайте и установите вручную:
curl -O https://raw.githubusercontent.com/kadwanev/bigboybrew/master/Library/Formula/sshpass.rb
brew install ./sshpass.rb
```

**Для CentOS/RHEL:**
```bash
sudo yum install sshpass
```
